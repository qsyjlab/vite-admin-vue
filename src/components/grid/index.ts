import { withInstall } from '@/utils'

import Grid from './src/index.vue'
import GridItem from './src/components/grid-item.vue'

export const VGrid = withInstall(Grid)
export const VGridItem = withInstall(GridItem)
