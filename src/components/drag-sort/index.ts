import { withInstall } from '@/utils'

import DragSort from './src/drag-sort.vue'
import DragSortItem from './src/drag-sort-item.vue'

export const VDragSort = withInstall(DragSort)

export const VDragSortItem = withInstall(DragSortItem)
